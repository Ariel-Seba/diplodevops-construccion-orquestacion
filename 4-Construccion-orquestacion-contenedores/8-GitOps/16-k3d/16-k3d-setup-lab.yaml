apiVersion: k3d.io/v1alpha5
kind: Simple
metadata:
  name: kube-lab
servers: 1
agents: 1
ports:
  - port: 8080:80
    nodeFilters:
      - server:*

options:
  k3d:
    wait: true
    timeout: "120s"
    disableLoadbalancer: false